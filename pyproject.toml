[build-system]
requires = ["setuptools>=61.0.0", "setuptools-scm>=8.0"]
build-backend = "setuptools.build_meta"

[project]
classifiers = [
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    
]
name = "nomad-pv-stability-measurements"
description = "data schema for ingesting stability measurments on pv according to different measurement protocals (standardized and custom)"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Simon Ternes", email = "john.doe@physik.hu-berlin.de" },
]
maintainers = [
    { name = "Simon Ternes", email = "john.doe@physik.hu-berlin.de" },
]
license = { file = "LICENSE" }
dependencies = [
    # A development build from the MPCDF index (`extra-index-url` below): the v2 base
    # sections need `Activity.datetime_end` and no `datetime` set by `BaseSection`.
    "nomad-lab>=1.4.4.dev144",
]

[project.urls]
Repository = "https://github.com/HU Berlin/nomad-pv-stability-measurements"

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
    "structlog",
    "mkdocs",
    "mkdocs-material==8.1.1",
    "pymdown-extensions",
    "mkdocs-click",
    "pytest-asyncio",
]

[tool.uv]
extra-index-url = [
  "https://gitlab.mpcdf.mpg.de/api/v4/projects/2187/packages/pypi/simple",
]
    
[tool.ruff]
# Exclude a variety of commonly ignored directories.
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "venv",
]

# Same as Black.
line-length = 88
indent-width = 4


[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # isort
    "I",
    # pylint
    "PL",
]

ignore = [
    "F403", # 'from module import *' used; unable to detect undefined names
    "PLC0415", # `import` should be at the top-level of a file
    "E501", # line too long
]

fixable = ["ALL"]

# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["PLC0415"] # import-outside-top-level

# this is entirely optional, you can remove this if you wish to
[tool.ruff.format]
# use single quotes for strings.
quote-style = "single"

# indent with spaces, rather than tabs.
indent-style = "space"

# Like Black, respect magic trailing commas.
skip-magic-trailing-comma = false

# Like Black, automatically detect the appropriate line ending.
line-ending = "auto"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[project.entry-points.'nomad.plugin']
parser_entry_point = "nomad_pv_stability_measurements.parsers:parser_entry_point"
measurement_parser_entry_point = "nomad_pv_stability_measurements.parsers:measurement_parser_entry_point"
schema_package_entry_point = "nomad_pv_stability_measurements.schema_packages:schema_package_entry_point"
normalizer_entry_point = "nomad_pv_stability_measurements.normalizers:normalizer_entry_point"
example_upload_entry_point = "nomad_pv_stability_measurements.example_uploads:example_upload_entry_point"
simulated_runs_example_upload_entry_point = "nomad_pv_stability_measurements.example_uploads:simulated_runs_example_upload_entry_point"
custom_protocols_example_upload_entry_point = "nomad_pv_stability_measurements.example_uploads:custom_protocols_example_upload_entry_point"
protocols_in_run_files_example_upload_entry_point = "nomad_pv_stability_measurements.example_uploads:protocols_in_run_files_example_upload_entry_point"

[tool.cruft]
# Avoid updating workflow files, this leads to permissions issues
skip = [".github/*"]
